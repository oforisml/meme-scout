# TEMPORARY — delete once it has answered its one question.
#
# Run #1 of ci.yml returned startup_failure with no logs, no annotations and no
# job, while actionlint reported the file clean and Actions is enabled on the
# repo. That leaves two possibilities that cannot be told apart from here: the
# workflow file, or the account.
#
# This is the smallest workflow GitHub will accept — no checkout, no
# setup-node, no concurrency, no permissions, no expressions, no third-party
# actions. If it ALSO fails to start, nothing in any file is the cause and the
# problem is account-level. If it runs, ci.yml is genuinely at fault despite
# every local check passing.
name: canary

on: [push, workflow_dispatch]

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo ok
